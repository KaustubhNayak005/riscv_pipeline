# Legacy helper kept for older notes. The offline flow now creates a fresh
# project and applies constraints automatically.
puts "Constraints are applied by scripts/run_offline_bringup.tcl."
puts "Run: vivado -mode batch -source scripts/run_offline_bringup.tcl"
exit
